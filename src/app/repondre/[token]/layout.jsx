// Page privée (lien reçu par le gérant) : jamais indexée par Google.
export const metadata = { title: "Réponse à valider — Niv Création", robots: { index: false, follow: false } };
export default function Layout({ children }) { return children; }
