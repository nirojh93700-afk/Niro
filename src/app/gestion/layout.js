// Métadonnées du raccourci "Gestion" (titre + icône d'écran d'accueil + non indexé).
export const metadata = {
  title: "Gestion — Niv Création",
  appleWebApp: { capable: true, title: "Gestion", statusBarStyle: "default" },
  robots: { index: false, follow: false },
};

export default function GestionLayout({ children }) {
  return children;
}
